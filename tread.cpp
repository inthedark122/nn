#include "tread.h"

Thread::Thread(QMainWindow* parent) : QThread() {
  //parent->set_text(12);
  //tmp = parent;
  //int i =10;
  //tmp->ui->textEdit;
  //set_text(12);
}

void Thread::run() {
  for (double i = 0; i < 100; i++) {
    //text->append(QString::number(i));
      //set_text(i);
      //textEdit->append("hello");

  }
}
